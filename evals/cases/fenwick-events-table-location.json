{
  "id": "fenwick-events-table-location",
  "split": "search",
  "fixture": "data-pipeline",
  "prompt": "In this workspace, which file defines the fct_events table? Answer with the file path only.",
  "note": "schema/events.sql holds the CREATE TABLE for fct_events; docs/conventions.md states the table-to-file mapping verbatim.",
  "expect": [
    {
      "metric": "run_completed"
    },
    {
      "metric": "answer_contains",
      "needle": "schema/events.sql"
    },
    {
      "metric": "turns_within",
      "max": 8
    }
  ]
}
