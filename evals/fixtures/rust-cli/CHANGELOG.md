# Changelog

## 0.3.1

- Fix a panic when searching with an empty query.

## 0.3.0

- Add `--tags` to `add`, comma-separated.

## 0.2.0

- `search` is now case-insensitive on both sides.

## 0.1.0

- First release: `add`, `list`, `search`.
