# Changelog

## 1.4.2

- Fix duplicate observations being stored when the same sighting is logged twice in a row.

## 1.4.1

- Fix `find` ignoring the location field; it now matches species and location.

## 1.2.0

- Add the `find` subcommand, backed by `find_observations`.

## 1.0.0

- First release: `log` and `list`.
